name: 'a1z26'
version: '1.1.0'
#  Module names should be the same as in a requirements.txt file.
requirements: ''
# Config will be read and saved to plugins_config.yaml
config:
  uses keys: false  # If true, should also set a `default key` value
  can change alphabet: false  # If, true, should also set an `alphabet` value

